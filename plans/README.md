# Implementation Plans

Generated by the improve skill on 2026-08-19 against commit `b40daa4`.
Execute in the order below unless dependencies say otherwise. Each executor
must read its plan fully, honor its STOP conditions, run every verification
gate, and update its status row when finished.

## Execution order & status

| Plan | Title | Source finding | Priority | Effort | Depends on | Status |
|------|-------|----------------|----------|--------|------------|--------|
| 001 | Make GA4 and Consent Mode initialize safely on every page | Finding 1 | P1 | S | — | TODO |
| 002 | Make Netlify the single production deployment contract | Finding 3 | P1 | S | 001 | TODO |
| 003 | Confine the preview server to the project and loopback | Finding 4 | P1 | S | 001 | TODO |
| 004 | Make local preview reproduce clean routes and explicit form simulation | Finding 5 | P2 | S | 002, 003 | TODO |
| 005 | Add one-command verification and gate deployment | Finding 2 | P1 | M | 001–004 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with one-line
reason) | `REJECTED` (with one-line rationale).

## Dependency notes

- Plan 001 is first because GA4 currently throws on both legal pages and stops
  the remainder of the shared script.
- Plan 002 must precede preview parity: the local contract should emulate the
  hosting platform that is actually authoritative.
- Plan 003 establishes a safe, importable preview handler before Plan 004 adds
  routing and form behavior to it.
- Plan 005 comes last so its permanent browser suite can encode the settled
  production and preview contracts instead of testing a temporary state.
- Plan 001 still adds a narrow standard-library regression test immediately;
  the urgent production fix does not wait for the full test-tooling plan.

## Findings considered and rejected

- Migrating this seven-page site to React, Next.js, or another application
  framework: not worth the runtime and maintenance cost for a static brochure
  site; a small verifier and optional build-time partials are sufficient.
- Splitting `styles.css` solely because it has 2,434 lines: no measured loading
  or maintenance failure justifies the churn yet; measure first.
- Enforcing a strict CSP immediately: rejected as part of these five plans
  because inline consent bootstrap, GA4, Google Fonts, and Maps require a
  separate report-only rollout and browser verification.
- Pinning GitHub Pages actions to commit SHAs: conditional and unnecessary if
  Plan 002 retires the Pages workflow; revisit only if Pages is intentionally
  retained.
- Removing existing `FAQPage` schema: Google may ignore it for a normal
  business site, but it is not a penalty and removal has negligible value.

